extends layout

block content
  h1= title
  p(id="welcome") Welcome to #{title}!
  <form action="">
  <input id="username" autocomplete="off" /><button>Send</button>
  </form>
  p(id="clock")
  h2 Connected Users
  ul(id="userList")
  button(type="button", id="startGameButton") Start Game!